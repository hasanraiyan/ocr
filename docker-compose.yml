version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: docusense-backend
    ports:
      - "8000:8000"
    volumes:
      - uploads-data:/app/uploads
    env_file:
      - ./backend/.env
    restart: always

volumes:
  uploads-data:
